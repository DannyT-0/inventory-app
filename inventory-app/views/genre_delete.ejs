<%- include('layout') %>

<h1><%= title %>: <%= genre.name %></h1>

<% if (genre_movies.length) { %>
<p>
	<strong
		>Delete the following movies before attempting to delete this genre.</strong
	>
</p>
<div style="margin-left: 20px; margin-top: 20px">
	<h4>Movies</h4>
	<dl>
		<% genre_movies.forEach(function(movie) { %>
		<dt>
			<a href="<%= movie.url %>"><%= movie.title %></a>
		</dt>
		<dd><%= movie.summary %></dd>
		<% }); %>
	</dl>
</div>
<% } else { %>
<p>Do you really want to delete this Genre?</p>
<form method="POST">
	<div class="form-group">
		<input
			id="genreid"
			class="form-control"
			type="hidden"
			name="genreid"
			value="<%= genre._id %>"
		/>
	</div>
	<button class="btn btn-primary" type="submit">Delete</button>
</form>
<% } %>
